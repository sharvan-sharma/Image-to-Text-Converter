#prototype 1 is just simple cinverter which writes the pixel vale to text file
#Prototype 2 is the usable solution and tested 
#prototype 2.1 is faster than prototype2 
#prototype 2.2 is similar to 2.1 but highly less data consumable file is generated when most of the pixels have same data value
